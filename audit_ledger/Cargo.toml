[package]
name = "audit_ledger"
version = "0.1.0"
edition = "2021"

[dependencies]
core_crypto = { path = "../core_crypto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"

[dev-dependencies]
fastrand = "2"

