[package]
name = "decision_twin"
version = "0.1.0"
edition = "2024"

[dependencies]
audit_ledger = { version = "0.1.0", path = "../audit_ledger" }
authority_gate = { version = "0.1.0", path = "../authority_gate" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tokio = { version = "1.53.1", features = ["full"] }
tracing = "0.1.44"
