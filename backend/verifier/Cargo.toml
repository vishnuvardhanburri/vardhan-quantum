[package]
name = "verifier"
version = "0.1.0"
edition = "2024"

[dependencies]
aes-gcm = "0.11.1"
bytes = "1.12.1"
core_crypto = { version = "0.1.0", path = "../core_crypto" }
proxy_engine = { version = "0.1.0", path = "../proxy_engine" }
tokio = { version = "1.53.1", features = ["full"] }
