[package]
name = "pq_shield"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.38", features = ["full"] }
tracing = "0.1"
core_crypto = { path = "../core_crypto" }
proxy_engine = { path = "../proxy_engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[[bench]]
name = "ingress_throughput"
harness = false
